const fullName = 'Miguel Zacharias da Silva';

const stringToArray = fullName.split(' ');

const username = 'Ola, ' + stringToArray[0] + ' ' + stringToArray[stringToArray.length - 1];

console.log(username);

